<?xml version="1.0" encoding="utf-8" ?>
<page:BaseContentPage x:Class="Jellyfin.Maui.Pages.HomePage"
                      xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                      xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
                      xmlns:page="clr-namespace:Jellyfin.Maui.Pages.Facades"
                      xmlns:vm="clr-namespace:Jellyfin.Maui.ViewModels;assembly=Jellyfin.Mvvm"
                      x:DataType="vm:HomeViewModel"
                      x:TypeArguments="vm:HomeViewModel">
    <ScrollView>
        <StackLayout Padding="16">
            <ActivityIndicator IsRunning="{Binding Loading}" />
            <CollectionView IsVisible="{Binding Loading, Converter={mct:InvertedBoolConverter}}"
                            ItemTemplate="{StaticResource HomeRowTemplateSelector}"
                            ItemsSource="{Binding HomeRowCollection, Mode=OneWay}"
                            ItemsUpdatingScrollMode="KeepLastItemInView" />
        </StackLayout>
    </ScrollView>
</page:BaseContentPage>
